plugins {
    id 'application'
}

dependencies {
    implementation project(':Earth')
    implementation project(':Mars')
}

run {
    // this is another way to define a task in gradle directly which works as expected.
    mainClass = 'sample.HelloWorlds'
}

